import { Box } from "@mui/material";
import React from "react";
import Layout from "../../layout";

export default function Index(props) {
  return (
    <Layout>
      <Box mt={10}>Tracker</Box>
    </Layout>
  );
}
